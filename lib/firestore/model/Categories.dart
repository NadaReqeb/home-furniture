class Categories {
  String catId;
  String name;

  Categories({this.catId, this.name});

  Categories.fromMap(Map map) {
    this.name = map['name'];
    this.catId = map['catId'];
  }
  toMap() {
    return {'name': this.name, 'catId': this.catId};
  }
}






















































































































